<h1>Editing Prerequisite</h1>

<%= error_messages_for :prerequisite_template %>

<% form_for :prerequisite_template, @prerequisite_template, :url => prerequisite_template_path(@prerequisite_template.ppos_template.degree_program.department, @prerequisite_template.ppos_template.degree_program, @prerequisite_template.ppos_template), :html => { :method => :put} do |f| %>
  <p>
    <b>Prerequisite</b><br />
    <%= f.text_field :prerequisite %>
  </p>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<p>
  <%= link_to 'Cancel', ppos_template_path(@prerequisite_template.ppos_template.degree_program.department, @prerequisite_template.ppos_template.degree_program, @prerequisite_template.ppos_template) %>
</p>
