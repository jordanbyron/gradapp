page[:notes].replace_html :partial => 'graduate_applicants/notes', 
                             :locals  => {:notes => @graduate_applicant.notes,
                                          :graduate_applicant    => @graduate_applicant}
page[:note_text].clear
page[@note].highlight