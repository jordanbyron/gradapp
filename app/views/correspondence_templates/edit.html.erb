<h1>Editing Correspondence Template</h1>

<%= error_messages_for :correspondence_template %>

<% form_for(@correspondence_template) do |f| %>
  <p>
    <b>Title</b><br />
    <%= f.text_field :title %>
  </p>

  <p>
    <b>Body</b><br />
    <%= f.text_area :body %>
  </p>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @correspondence_template %> |
<%= link_to 'Back', correspondence_templates_path %>
