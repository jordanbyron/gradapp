<h1>New Header</h1>

<%= error_messages_for :header_template %>

<% form_for(@header_template) do |f| %>
  <p>
    <b>Header</b><br />
    <%= f.text_field :header %>
  </p>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Cancel', ppos_template_path(@department.id, @degree_program.id,@ppos_template.id) %>
